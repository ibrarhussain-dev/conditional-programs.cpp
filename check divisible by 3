#include <iostream>
using namespace std;
int main() {
    int num;
    cout<<"enter the number : ";
    cin>>num;
    if( num % 3 == 0 ){
        cout<<"it is divisible."<<endl;
    }
else
cout<<"not divisible";
}
